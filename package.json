{
  "name": "@workspace/root",
  "private": true,
  "scripts": {
    "start:api:dev": "cd ./packages/api && npm run start:dev",
    "start:app:web:dev": "cd ./packages/app && npm run web"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/.prisma",
      "**/.prisma/**",
      "**/@prisma",
      "**/@prisma/**",
      "**/prisma",
      "**/prisma/**"
    ]
  }
}
